[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pythonoffice"
version = "1.0.0"
description = "A Microsoft Office-inspired desktop suite built with Python and Tkinter."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pythonoffice = "main:main"
